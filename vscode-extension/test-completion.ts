/**
 * CodeYang VS Code Extension - Test File
 *
 * 用于测试多行补全功能
 */

// ==========================================
// 测试 1: 单行补全
// ==========================================
// 输入: const add = (a: number, b: number
// 按空格触发补全，期望自动补全参数和返回类型

// ==========================================
// 测试 2: 函数补全（多行）
// ==========================================
// 输入: function calculateTotal(items: Item[]) {
// 按回车触发补全，期望补全完整函数体

// ==========================================
// 测试 3: 类定义补全
// ==========================================
// 输入: class UserService {
// 按回车触发补全，期望补全完整类结构

// ==========================================
// 测试 4: 异步函数补全
// ==========================================
// 输入: async function fetchUser(id: string) {
// 期望补全 async/await 逻辑

// ==========================================
// 测试 5: 接口补全
// ==========================================
interface User {
  id: string;
  name: string;
  email: string;
}

// 基于上述 User 接口，测试类型感知
// 输入: function getUser(

// ==========================================
// 测试 6: 控制流补全
// ==========================================
// 输入: if (user && user.isActive) {
// 期望补全完整的 if 块

// ==========================================
// 测试说明
// ==========================================
/*
触发方式：
1. 自动触发 - 输入代码后等待 300ms
2. 手动触发 - Ctrl+Shift+Space
3. 接受补全 - Tab 或 Enter
4. 拒绝补全 - Esc

观察要点：
- 响应速度（首次 500-1000ms，缓存 <50ms）
- 补全质量（语法正确、类型安全）
- 多行对齐（缩进一致）
- 上下文感知（使用已定义类型）
*/
