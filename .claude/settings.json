{
  "allowedCommands": [
    {
      "tool": "Bash",
      "match": "**",
      "note": "Allow all bash commands"
    },
    {
      "tool": "PowerShell",
      "match": "**",
      "note": "Allow all PowerShell commands"
    }
  ],
  "allowedPaths": {
    "read": ["**"],
    "write": ["**"],
    "readOrWrite": ["**"]
  },
  "mcpServers": {}
}
